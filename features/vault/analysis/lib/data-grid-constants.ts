/**
 * Column sizing constants for the data grid
 */
export const COLUMN_SIZE = {
  /** Minimum width a column can be resized to (in pixels) */
  MIN: 64,
  
  /** Default width for newly created columns (in pixels) */
  DEFAULT: 120,
  
  /** Maximum width a column can be resized to (in pixels) */
  MAX: 500,
} as const;

